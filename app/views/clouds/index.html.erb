<%= provide(:title, 'All clouds') %>
<h1>All clouds</h1>

<%= render 'button-nav' %>

<%= will_paginate %>

<table class="data" cellpadding="0" cellspacing="0" border="0">
	<tr>
		<th>Cloud name</th>
		<th>&nbsp;</th>
	</tr>
	<%= render @clouds %>
</table>

<%= will_paginate %>

<%= render 'button-nav' %>