<%= provide(:title, 'All clouds') %>
<h1>All clouds</h1>

<%= will_paginate %>

<div>
	<%= link_to "&rsaquo; Add new cloud".html_safe, new_cloud_path %>
</div>

<table class="data" cellpadding="0" cellspacing="0" border="0">
	<tr>
		<th>Cloud name</th>
		<th>&nbsp;</th>
	</tr>
	<%= render @clouds %>
</table>

<%= will_paginate %>


<%= @matches %>