<%= provide(:title, 'All cloud matches') %>

<div id="save-alert" class="alert alert-success"></div>
<h1>All cloud matches</h1>
<p>The priority order of the match list can be sorted using drag &amp; drop. Just click "Save sort order" to save your changes.</p>

<%= render 'button-nav' %>

<table class="data sortable" cellpadding="0" cellspacing="0" border="0">
	<thead>
		<tr>
			<th>Match</th>
			<th>Match list</th>
			<th>Cloud</th>
			<th>&nbsp;</th>
		</tr>
	</thead>
	<tbody>
		<%= render @cloud_matches %>
	</tbody>
</table>

<%= render 'button-nav' %>