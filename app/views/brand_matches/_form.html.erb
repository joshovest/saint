<%= form_for(@brand_match) do |f| %>
  <% if @brand_match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brand_match.errors.count, "error") %> prohibited this brand_match from being saved:</h2>

      <ul>
      <% @brand_match.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :match_list %><br />
    <%= f.text_field :match_list %>
  </div>
  <div class="field">
    <%= f.label :exclude_list %><br />
    <%= f.text_field :exclude_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
