<%= provide(:title, 'Manage users') %>
<h1>Manage users</h1>

<%= will_paginate %>

<div>
	<%= link_to "&rsaquo; Add new user".html_safe, new_user_path %>
</div>

<table class="data" cellpadding="0" cellspacing="0" border="0">
	<tr>
		<th>User</th>
		<th>&nbsp;</th>
	</tr>
	<%= render @users %>
</table>

<%= will_paginate %>