<%= provide(:title, 'Manage users') %>
<h1>Manage users</h1>

<%= render 'button-nav' %>

<%= will_paginate %>

<table class="data" cellpadding="0" cellspacing="0" border="0">
	<tr>
		<th>User</th>
		<th>&nbsp;</th>
	</tr>
	<%= render @users %>
</table>

<%= will_paginate %>

<%= render 'button-nav' %>